// Optional: Add click functionality for Quick View buttons
document.querySelectorAll('.quick-view').forEach(button => {
    button.addEventListener('click', function() {
        alert('Quick View feature coming soon!');
    });
});